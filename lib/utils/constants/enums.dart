enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delivered }

enum PaymentMethods { creditCard, visa, masterCard, paypay, cash }

enum ProductType { food, clothes, drink, electronic, services }
